import Foundation

// Entities are simple data objects, without business logic, that are managed by the Interactor and never passed to the Presenter.

class ChoiceAuthMethodEntity {
        
    init() {
        
    }
}
